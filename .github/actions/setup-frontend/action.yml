name: Setup Frontend
description: >
  Install Node.js, pnpm, and project dependencies.
  Reusable composite action for all frontend jobs.

inputs:
  node-version:
    description: Node.js version
    default: '22'
  install-playwright:
    description: Install Playwright browsers after deps
    default: 'false'
  playwright-browsers:
    description: Which browsers to install (e.g. "chromium firefox webkit")
    default: 'chromium'

runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
      with:
        node-version: ${{ inputs.node-version }}

    - name: Setup pnpm
      uses: pnpm/action-setup@b0f76dfb45f55f8421693e4803ac7bb65143bd34

    - name: Install project dependencies
      shell: bash
      run: pnpm install --frozen-lockfile

    - name: Install Playwright browsers
      if: inputs.install-playwright == 'true'
      shell: bash
      run: pnpm --filter app exec playwright install --with-deps ${{ inputs.playwright-browsers }}
