name: Semantic PR
permissions:
  contents: read
  pull-requests: write  # Required by the reusable semantic-pr.yml to post sticky PR status comments
on:
  pull_request_target:
    types: [opened, edited, synchronize]
jobs:
  main:
    uses: dallay/common-actions/.github/workflows/semantic-pr.yml@69060250307677655136ca88c70a2f77cb4153b2 # v2.2.4
