name: Semantic PR
permissions:
  contents: read
  pull-requests: write  # Required by the reusable semantic-pr.yml to post sticky PR status comments
on:
  pull_request_target:
    types: [opened, edited, synchronize]
jobs:
  main:
    uses: dallay/common-actions/.github/workflows/semantic-pr.yml@87129475c06d9f3354d398426926ba4345a4644d # v2.2.3
