# Keep CodeQL focused on the backend and shared modules.
# We intentionally rely on the workflow's default query suite for now instead of
# narrowing queries further, so reviewers get the maintained GitHub baseline.
name: profiletailors-backend-scope

paths:
  - server/smp
  - shared

paths-ignore:
  - apps/web/marketing
  - docs
  - openspec
  - tmp
