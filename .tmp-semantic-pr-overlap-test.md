# TEMPORARY FILE — Semantic PR overlap acceptance test

This file exists ONLY to give the `test/semantic-pr-overlap` branch a
commit so we can open a disposable PR and generate overlapping
`pull_request_target` `edited` events against the released workflow
`dallay/common-actions@87129475c06d9f3354d398426926ba4345a4644d` (v2.2.3).

It will be deleted when the PR is closed. It must never be merged.