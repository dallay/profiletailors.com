include:
  - ../common.yml
services:
  wiremock:
    image: 'wiremock/wiremock:3.12.1'
    container_name: profiletailors-wiremock
    command:
      - '--port'
      - '${WIREMOCK_PORT:-8080}'
      - '--global-response-templating'
      - '--verbose'
    environment:
      WIREMOCK_OPTIONS: '${WIREMOCK_OPTIONS:-}'
    ports:
      - '${WIREMOCK_PORT:-8080}:${WIREMOCK_PORT:-8080}'             # WireMock HTTP
      - '${WIREMOCK_HTTPS_PORT:-8443}:${WIREMOCK_HTTPS_PORT:-8443}'  # WireMock HTTPS (optional)
    volumes:
      - ./linkedin/__files:/home/wiremock/__files:ro
      - ./linkedin/mappings:/home/wiremock/mappings:ro
    healthcheck:
      test: [ "CMD", "curl", "--fail", "http://localhost:${WIREMOCK_PORT:-8080}/__admin/health" ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - profiletailors
