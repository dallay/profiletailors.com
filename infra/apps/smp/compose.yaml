include:
  - ../../postgres/compose.yaml
  - ../../monitoring/compose.yaml

# Este compose incluye:
# - PostgreSQL (desde infra/postgres)
# - Prometheus + Grafana (desde infra/monitoring)
#
# Para iniciar todo el stack de SMP:
#   docker compose -f infra/apps/smp/compose.yaml up -d
#
# Para detener:
#   docker compose -f infra/apps/smp/compose.yaml down
