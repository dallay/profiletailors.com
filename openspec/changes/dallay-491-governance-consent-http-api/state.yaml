change: dallay-491-governance-consent-http-api
current_phase: apply
completed:
  - explore
  - propose
  - spec
  - design
  - tasks
  - apply
next: verify
note: |
  Single-PR size:exception approved. Implementation covers the trimmed task list:
  - Append-only consent_record_events ledger + partial unique index on consent_records
  - Atomic recordActiveReturning / withdrawActiveReturning inside one R2DBC transaction
  - RecordConsentOutcome(created, record) for 201/200 semantics
  - 4 HTTP endpoints (record/withdraw/list/history) with workspace context derived from
    ResourceContextProvider, no workspaceId or principalId in the request
  - workspace:consent:read permission seeded for WORKSPACE_OWNER
  - Problem Details for 400 (enum/locale) and 404 (missing record)
  - Strict TDD: ConsentControllerWebTest (9 scenarios), updated handler tests
  - just backend-check passes (Detekt + tests + Kover)
updated: 2026-07-20T10:40:00.000Z
