change: semantic-pr-cancellation-source-fix
current_phase: archived
completed:
  - init
  - explore
  - propose
  - spec
  - design
  - tasks
  - apply
  - verify
  - qa
  - archive
next: none
updated: 2026-08-15
warnings:
  - Verify re-run complete on 2026-08-15: verdict PASS WITH WARNINGS (replaces prior FAIL). All prior CRITICAL items resolved with live evidence: task 4.1 overlap burst on PR #803 (runs 31884345568/31884374535/31884376688/31884377801/31884379096) proves no started job is cancelled (cancelled runs have steps: []); valid/invalid title and burst scenarios COMPLIANT; consumer YAML final-newline lint resolved (yamllint -d relaxed exits 0). Remaining WARNINGS: different-PR isolation runtime gap (PARTIAL, static diff evidence), mutable-pin negative test (PARTIAL), QA-002, QA-003.
  - QA re-run complete on 2026-08-15: verdict PASS WITH WARNINGS. QA-001 (CRITICAL) RESOLVED as a false negative — the prior test ran before PR #801 merged, so pull_request_target executed base-branch main which still pinned v2.2.2 (cancel-in-progress: true); post-fix live evidence on v2.2.3 shows no started job is cancelled.
  - QA-002 (P2) WAIVED on 2026-08-15 by explicit repository-operator decision: the separate @dallay/infra review evidence for the v2.2.3 source release is waived. Rationale recorded in qa-report.md Findings: single-line cancel-in-progress policy flip, validated by live post-merge overlap evidence, consumer pin independently verified against the released tag. All acceptance blockers are resolved; QA-003 (P3, pre-existing source yamllint debt) remains documented and does not block archive.
  - QA-003 remains OPEN (P3): pre-existing source YAML lint debt in common-actions semantic-pr.yml.
  - PR #801 is MERGED to main (2026-08-15T11:49:24Z); task 4.1 live evidence obtained via disposable PR #803; consumer YAML final-newline lint blocker resolved.
  - ARCHIVED on 2026-08-15: change folder moved to openspec/changes/archive/2026-08-15-semantic-pr-cancellation-source-fix/. No change-local delta-spec directory existed (openspec/changes/semantic-pr-cancellation-source-fix/specs/ absent); the applicable contract is the main spec openspec/specs/semantic-pr-validation/spec.md, which was authored by this change and already encodes the accepted behavior (PR-scoped concurrency group with cancel-in-progress: false semantics, immutable released SHA pin, opened/edited/synchronize validation) — no main-spec merge was required. verify-report.md (PASS WITH WARNINGS, 5/7 COMPLIANT, 2/7 PARTIAL, 0 UNTESTED) and qa-report.md (PASS WITH WARNINGS; QA-001 resolved as false negative; QA-002 P2 WAIVED by explicit repository-operator decision on 2026-08-15; QA-003 P3 pre-existing source lint debt) are preserved in the archive as acceptance evidence. NOT TESTED scenarios S-06 (synchronize), S-09 (different-PR isolation), and S-10 (unauthorized identity) remain documented with rerun prerequisites and do not block archive.