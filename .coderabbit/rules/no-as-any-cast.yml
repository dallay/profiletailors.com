id: no-as-any-cast
language: TypeScript
severity: error
message: |
  Avoid `as any` casts. They erase type safety and are a frequent source of
  runtime bugs. Use proper typing or `as unknown as T` with a real narrowing.
rule:
  pattern: '$EXPR as any'
note: |
  See AGENTS.md TypeScript conventions.
