spring:
  application:
    name: smp
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${SMP_JWT_ISSUER_URI:http://localhost:8081/realms/profiletailors}
          jwk-set-uri: ${SMP_JWT_JWK_SET_URI:http://localhost:8081/realms/profiletailors/protocol/openid-connect/certs}
  r2dbc:
    url: ${SMP_R2DBC_URL:r2dbc:postgresql://localhost:5432/profiletailors_smp}
    username: ${SMP_DB_USERNAME:profiletailors}
    password: ${SMP_DB_PASSWORD:profiletailors}
  liquibase:
    enabled: ${SMP_LIQUIBASE_ENABLED:true}
    change-log: classpath:db/changelog/db.changelog-master.yaml
    url: ${SMP_LIQUIBASE_JDBC_URL:jdbc:postgresql://localhost:5432/profiletailors_smp}
    user: ${SMP_DB_USERNAME:profiletailors}
    password: ${SMP_DB_PASSWORD:profiletailors}

platform:
  workspace-context:
    header-name: ${SMP_WORKSPACE_HEADER_NAME:X-Workspace-Id}
  hooks:
    audit:
      enabled: ${SMP_PLATFORM_AUDIT_ENABLED:false}
    metrics:
      enabled: ${SMP_PLATFORM_METRICS_ENABLED:false}
    rate-limit:
      enabled: ${SMP_PLATFORM_RATE_LIMIT_ENABLED:false}

---
spring:
  config:
    activate:
      on-profile: test
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${SMP_TEST_JWT_ISSUER_URI:http://localhost/test-issuer}
          jwk-set-uri: ${SMP_TEST_JWT_JWK_SET_URI:http://localhost/test-jwks}
  r2dbc:
    url: ${SMP_TEST_R2DBC_URL:r2dbc:postgresql://localhost:5432/profiletailors_smp_test}
    username: ${SMP_TEST_DB_USERNAME:profiletailors}
    password: ${SMP_TEST_DB_PASSWORD:profiletailors}
  liquibase:
    enabled: ${SMP_TEST_LIQUIBASE_ENABLED:false}
