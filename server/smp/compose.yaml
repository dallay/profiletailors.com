# Delegates to the centralized infrastructure compose
# All services are defined in infra/apps/smp/compose.yaml
include:
  - ../../infra/apps/smp/compose.yaml

# This file provides backward compatibility for:
#   docker compose up -d
# when run from the server/smp/ directory.
#
# Automatically includes:
# - PostgreSQL
# - Prometheus + Grafana
# - Mailpit (fake SMTP server for local development)
