# Usar la infraestructura centralizada
include:
  - ../../infra/apps/smp/compose.yaml

# Este archivo mantiene compatibilidad con el comando:
#   docker compose up -d
# desde el directorio server/smp/
#
# Incluye automáticamente:
# - PostgreSQL
# - Prometheus
# - Grafana
