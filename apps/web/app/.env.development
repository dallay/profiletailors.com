# Development: API calls go through Vite proxy (same origin).
# Set to empty string so fetch() uses relative URLs like /api/...
# Vite server proxies /api/* requests to the backend (see vite.config.ts).
VITE_API_BASE_URL=
