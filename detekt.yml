config:
  validation: true

style:
  active: true
  excludes:
    - '**/test/**'

performance:
  active: true
  excludes:
    - '**/test/**'

potential-bugs:
  active: true
  excludes:
    - '**/test/**'

naming:
  active: true
  excludes:
    - '**/test/**'

empty-blocks:
  active: true
  excludes:
    - '**/test/**'

complexity:
  active: true
  excludes:
    - '**/test/**'
  LongMethod:
    allowedLines: 90
  LargeClass:
    allowedLines: 500
